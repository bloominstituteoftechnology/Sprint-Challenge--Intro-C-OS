#include <stdio.h>
#include <dirent.h>

/**
 * Main
 */
int main(int argc, char **argv)
{
  // Parse command line

  // Open directory
  opendir()

      // Repeatly read and print entries
      readdir()

      // Close directory
      closedir()

          return 0;
}